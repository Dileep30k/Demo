@model List<Edu.Abstraction.ComplexModels.SchemeInstituteModel>
@if (Model.Any())
{
    <div class="row">
        <div class="col-md-12">
            <div class="card card-primary">
                <div class="card-body tableDiv">
                    <table id="categoryVariantDatatable" class="table datatable">
                        <thead>
                            <tr>
                                <th>Institute </th>
                                <th></th>
                            </tr>
                        </thead>
                        @foreach (var institute in Model)
                        {
                            <tr>
                                <td>@institute.InstituteName</td>
                                <td><a href="javascript:void(0)" onclick="removeInstitute(@institute.SchemeInstituteId)"><i class="fas fa-trash"></i></a></td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
}
